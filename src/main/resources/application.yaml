spring:
  application:
    name: testTask
  datasource:
    url: jdbc:postgresql://localhost:5432/taskDB
    username: usr
    password: pwd
  flyway:
    enabled: true

logging:
  level:
    org.springframework.jdbc.core.JdbcTemplate: TRACE