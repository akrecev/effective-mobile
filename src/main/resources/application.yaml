spring:
  application:
    name: testTask
  datasource:
    url: jdbc:postgresql://localhost:5432/taskDB
    username: usr
    password: pwd
  flyway:
    enabled: true
  sql:
    init:
      mode: never

logging:
  level:
    org.springframework.jdbc.core.JdbcTemplate: TRACE